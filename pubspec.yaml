name: fluo
description: Authentication made for Flutter. Complete UI flows in less than 5 minutes.
version: 3.2.1
homepage: https://fluo.dev
repository: https://github.com/fluo-dev/flutter-sdk

environment:
  sdk: ">=3.5.1 <4.0.0"
  flutter: ">=3.8.0"

platforms:
  android:
  ios:
  web:
  macos:

topics:
  - authentication
  - authentication-sdk
  - user-onboarding
  - user-registration
  - social-login

dependencies:
  country_flags: ^3.3.0
  device_region: ^1.4.0
  diacritic: ^0.1.6
  flutter:
    sdk: flutter
  flutter_libphonenumber: ^2.5.1
  flutter_localizations:
    sdk: flutter
  flutter_secure_storage: ^9.2.2
  freezed_annotation: ^2.4.4
  google_sign_in: ^6.3.0
  google_sign_in_web: ^0.12.4+4
  http: ^1.2.2
  intl: ^0.20.2
  json_annotation: ^4.9.0
  pinput: ^5.0.0
  provider: ^6.1.2
  sign_in_with_apple: ^7.0.1
  styled_text: ^8.1.0
  url_launcher: ^6.3.1

dev_dependencies:
  build_runner: ^2.4.12
  flutter_lints: ^4.0.0
  flutter_test:
    sdk: flutter
  freezed: ^2.5.7
  json_serializable: ^6.8.0

flutter:
  generate: true
  assets:
    - assets/images/
